version: '3'
services:
  web:
    build: .
	ports:
	    - 8080:80
	volumes:
		- var/blog/blogapi/log:/app/log
	links:
		- db
  db:
     image: mysql:5.7
	 restart: always
	 ports:
	   - 3306:3306
	 container_name: mysql
	 environment:
	   MYSQL_ROOT_PASSWORD:"i<=2359loveN"
	 volumes:
	    - ./data:/var/lib/mysql

